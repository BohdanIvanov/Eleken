import { StyleSheet } from 'react-native';
import { styleColor } from 'app/colors';

export default StyleSheet.create({
  container: {
    position: 'relative',
    flex: 1,
    backgroundColor: styleColor.alabaster
  }
});
